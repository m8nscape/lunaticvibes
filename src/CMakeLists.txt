add_subdirectory(common)
add_subdirectory(config)
add_subdirectory(db)
add_subdirectory(game)

find_package(Qt5 COMPONENTS Widgets)
if (Qt5_FOUND)
    add_subdirectory(setup)
else()
    message(WARNING "Qt5 not found, skipping setup app")
endif()
